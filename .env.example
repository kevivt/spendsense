# Copy this file to .env and fill in real values.
# .env itself is gitignored — .env.example is safe to commit as a template.

# Only needed if you later add a paid API (not required for the local Ollama setup)
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
